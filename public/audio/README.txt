Optional background music.

Drop an MP3 named  bgm.mp3  into this folder and the site plays it (looped) instead of the
built-in synthesized nautical loop. Music on/off and volume are in Settings.

Only use music you have the rights to share (the built-in tune is original, so the app itself
ships nothing copyrighted).
